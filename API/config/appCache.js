const appConstants = require("./appConstants");
// redis = require('redis'),
//     url = require('url');
// var client = redis.createClient(appConstants.REDIS_URL, { no_ready_check: true });
// client.auth('LtqAcuqf&h^wQ', function(err) {
//     if (err) throw err;
// });

// client.on('connect', function() {
//     console.log('Connected to Redis');
// });

// client.on('error', function(e) {
//     console.log(e);
// });

//module.exports = client;